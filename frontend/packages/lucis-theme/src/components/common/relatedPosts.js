import React from "react"
import { connect } from "frontity"
import { BoxRoundedBlur } from "../../styles/common"

const RelatedPosts = ({ state }) => {
  return (
    <BoxRoundedBlur>RelatedPosts</BoxRoundedBlur>
  )
}

export default connect(RelatedPosts)